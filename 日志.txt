【2025年9月17日】：尝试将jQuery外部引用文件指向github，不能正常弹出面板。原音分析：可能是网络限制导致不能正常访问。jQuery的起“翻译”作用，将同一代码运用在不同浏览器上。

【2025年9月18日】：在控制台输入$.fn.jquery命令获取jquery库版本，版本过低需要引入高版本解决操作问题。

【2025年9月19日】：依赖网页jQuery库可能会导致弹窗加载太慢，直接在脚本中预先引入可以解决问题。
。
【2025年9月20日】：同步本地进度至github操作：①执行git clone http://*仓库地址*，直接克隆远程仓库到本地；②在 CMD 中执行以下命令：git config --global user.name "GitHub用户名"换行git config --global user.email "GitHub邮箱"；③在本地仓库根目录（与.git文件夹同级）放置auto-push.bat文件。

【2025年9月21日】：完成版本0.0.2编辑。

【2025年9月26日】：转变思路，将原来集成到一个文件中的脚本拆解为三个部分，user部分负责上传到篡改猴，触发答题模式引用外部脚本，同时加载弹窗样式，配备加载失败提醒，三个文件版本同时定义为v1.0.1。下一步将修复user中脚本正常运行时弹窗大致外观，版本号依旧是v1.0.1。